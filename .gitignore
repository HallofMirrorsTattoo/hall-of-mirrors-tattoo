# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage

# Next.js build output (anywhere in repo)
.next/
/out/
out/

# Production builds
/build
/dist
dist/

# TypeScript build info
*.tsbuildinfo

# Misc
.DS_Store
*.pem

# Environment files — never commit anything that could contain real values.
# frontend/.env.production is intentionally tracked but only ever holds the
# public NEXT_PUBLIC_API_URL. Real secrets live in Railway / Vercel env vars.
.env
.env.*
!.env.example
!.env.production
!.env.local.example
.env.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db
